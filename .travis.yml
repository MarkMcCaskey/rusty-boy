language: rust
sudo: true

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libsdl2-dev
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
    - rust: stable
